# Third-Party Notices

This release contains no third-party production npm dependencies.
All production package dependencies are internal `@remotish/*` workspace packages.

Build, test, and release tools are development-only and are not distributed as runtime dependencies.
