/** Adapter implementation for GitHub repositories using browser-safe REST and GraphQL APIs. */
export { GitHubAdapter } from './github-adapter.js';
/** Connection and authentication options accepted by the GitHub adapter. */
export type { GitHubAdapterOptions } from './types.js';
